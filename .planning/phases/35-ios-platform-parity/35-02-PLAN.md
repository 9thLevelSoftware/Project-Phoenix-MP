---
phase: 35
plan: 2
title: "iOS Infrastructure"
wave: 2
depends_on: [35-01]
agent: "Senior Developer"
must_haves:
  - "ConnectivityChecker returns real network status on iOS"
  - "withPlatformLock documented with performance notes"
findings: [H12, H13]
---

<objective>
Fix two HIGH-severity iOS infrastructure issues: the ConnectivityChecker stub that always returns true (H12), and the single global lock that serializes all callers (H13).
</objective>

<context>
## H12: ConnectivityChecker.ios.kt always returns true
- **File:** `shared/src/iosMain/kotlin/com/devil/phoenixproject/util/ConnectivityChecker.ios.kt`
- Returns `true` unconditionally with comment "Full iOS implementation would use NWPathMonitor"
- Sync operations guarded by `isOnline()` always attempted on iOS regardless of connectivity
- **Fix:** Implement using Network.framework NWPathMonitor via Kotlin/Native interop

## H13: withPlatformLock uses single global lock
- **File:** `shared/src/iosMain/kotlin/com/devil/phoenixproject/util/Locking.ios.kt`
- Ignores the `lock: Any` parameter, always uses `globalLock` (NSRecursiveLock)
- Comment explains rationale (WeakReference map leak avoidance)
- **Fix:** For v0.8.0, document the limitation and add performance notes. Consider using `@Synchronized` annotation or documenting which callers are time-sensitive. Full per-object locking is a v0.9.0 optimization.
</context>

<tasks>
## Task 1: Implement ConnectivityChecker with NWPathMonitor (H12)
Implement using Kotlin/Native interop with Network.framework. Use `NWPathMonitor` to track connectivity state.

## Task 2: Document withPlatformLock limitation (H13)
Add KDoc explaining the single-lock design decision, performance implications, and that per-object locking is deferred. Add TODO for v0.9.0.
</tasks>

<verification>
- Build compiles
- Code review: ConnectivityChecker uses NWPathMonitor
- Code review: Locking.ios.kt has clear documentation
</verification>

<success_criteria>
- Sync fails fast on airplane mode on iOS (H12)
- Global lock limitation documented with rationale (H13)
</success_criteria>

<output>
Plan summary: H12 implemented ConnectivityChecker with NWPathMonitor. H13 documented global lock limitation.
</output>
