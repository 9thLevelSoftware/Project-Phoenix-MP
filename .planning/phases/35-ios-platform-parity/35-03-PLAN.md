---
phase: 35
plan: 3
title: "Feature Gating"
wave: 2
depends_on: [35-01]
agent: "Senior Developer"
must_haves:
  - "Form Check camera button hidden on iOS"
  - "No Vico chart imports in commonMain or iosMain"
findings: [H14, H15]
---

<objective>
Fix two HIGH-severity iOS feature gating issues: the MediaPipe/CameraX form check feature that's Android-only with no iOS gate (H14), and the Vico chart library that could leak into shared code (H15).
</objective>

<context>
## H14: CV Form Check Android-only with no iOS gate
- **File:** Various shared UI screens that reference form check features
- MediaPipe `PoseLandmarkerHelper` and `LandmarkAngleCalculator` are in `androidMain` only
- No iOS equivalent exists (deferred per project scope)
- But the shared UI might show camera buttons or form check toggles on iOS
- **Fix:** Gate the form check UI behind a platform check. Use `expect/actual` for a `PlatformCapabilities` object, or check `Platform.isIos` before showing form check UI elements.

## H15: Vico chart library Android-only leakage
- **File:** `shared/build.gradle.kts` (line 151) — Vico in androidMain dependencies
- Need to verify no Vico imports exist in commonMain or iosMain
- **Fix:** Grep for Vico imports in commonMain/iosMain. If found, refactor to use platform-neutral alternatives. If none found, document the verification.
</context>

<tasks>
## Task 1: Gate Form Check feature on iOS (H14)
1. Search for form check UI references in commonMain screens
2. Add platform check to hide form check camera button/toggle on iOS
3. Use existing `Platform` expect/actual or add `isFormCheckAvailable` property

## Task 2: Verify Vico chart isolation (H15)
1. Grep for any Vico imports (`com.patrykandpatrick.vico`) in commonMain or iosMain
2. If found: refactor to platform-neutral alternative
3. If not found: document as verified — Vico correctly isolated to androidMain
</tasks>

<verification>
- Build compiles (both Android and iOS targets if possible)
- Grep: zero Vico imports in commonMain/iosMain
- Code review: form check UI gated behind platform check
</verification>

<success_criteria>
- Form Check camera button hidden on iOS (H14)
- iOS build compiles with no unresolved Vico references (H15)
</success_criteria>

<output>
Plan summary: H14 gated form check UI behind platform check. H15 verified Vico isolation. Phase 35 complete.
</output>
