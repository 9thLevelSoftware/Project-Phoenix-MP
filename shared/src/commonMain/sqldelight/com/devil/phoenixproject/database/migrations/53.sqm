-- Track whether a routine exercise's duration is authoritative for portal sync.
-- Existing rows remain unknown because older clients discarded pulled durations. The
-- sync layer includes their parent routine in the next push and omits the routine ID
-- from pull parity until an authoritative duration is received.
ALTER TABLE RoutineExercise
ADD COLUMN durationSyncKnown INTEGER NOT NULL DEFAULT 0;
